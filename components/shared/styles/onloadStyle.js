import { StyleSheet } from 'react-native';

const onloadStyle = StyleSheet.create({
    container: {
        flex: 1,
        justifyContent: "flex-start",
        alignItems: "center",
        backgroundColor: "black"
      },
    renderedImage: {
        height: 300,
        width: 300
    }


})

export default onloadStyle